import { Input as BaseInput } from "<ebay-notice-base>";

export type Input = Omit<BaseInput, 'prefixClass' | 'root' | 'headerRoot' | 'mainRoot' | 'noA11yLabel'>;

$ var status = input.status || "attention";

<ebay-notice-base
    ...input
    class=[`inline-notice--${status}`, input.class]
    status=status
    prefixClass="inline-notice"
    root="div"
    headerRoot="span"
    mainRoot="span"
    noA11yLabel
/>
