import french from "./french";
<ebay-phone-input
    floating-label="Date"
    onExpand("emit", "expand")
    onCollapse("emit", "collapse")
    onBlur("emit", "blur")
    onFocus("emit", "focus")
    onKeydown("emit", "keydown")
    onKeypress("emit", "keypress")
    onKeyup("emit", "keyup")
    onChange("emit", "change")
    ...input
    country-names=french
/>
