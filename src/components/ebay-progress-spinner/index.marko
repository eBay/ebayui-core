import processHtmlAttributes from "../../common/html-attributes"

static var ignoredAttributes = [
    "class",
    "size"
];

<span ...processHtmlAttributes(input, ignoredAttributes) class=[ "spinner", input.size === "large" && "spinner--large", input.class] role="img"></span>
