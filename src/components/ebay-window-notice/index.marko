import processHtmlAttributes from "../../common/html-attributes"

$ var status = "confirmation";
<ebay-notice-base
    class=[input.window === "fullscreen" && `window-notice--screen`, input.class]
    role="region"
    status=status
    prefixClass="window-notice"
    ...input/>
