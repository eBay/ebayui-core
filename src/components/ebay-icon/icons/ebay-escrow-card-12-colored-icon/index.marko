import { symbol } from "./symbol.js";
import type { Input as IconInput } from "../../component-browser"
export type Input = Omit<IconInput, `_${string}`>;
<ebay-icon ...input _name="escrow-card-12-colored" _type="icon" _themes=symbol/>
