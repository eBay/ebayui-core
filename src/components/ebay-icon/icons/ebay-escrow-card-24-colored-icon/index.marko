import { symbol } from "./symbol";
import type { Input as IconInput } from "../../component"
export type Input = Omit<IconInput, `_${string}`>;
<ebay-icon ...input _name="escrow-card-24-colored" _type="icon" _themes=symbol/>
