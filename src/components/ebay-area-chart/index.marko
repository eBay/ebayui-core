import { processHtmlAttributes } from "../../common/html-attributes";
$ const {
    data,
    class: inputClass,
    series,
    title,
    description,
    xLabelFormatter,
    yLabelFormatter,
    highchartOptions,
    // xAxisLabelFormat,
    // yAxisLabels,
    // xAxisPositioner,
    // yAxisPositioner,
    ...restInput
} = input;

<div ...processHtmlAttributes(restInput) class=[input.class, "ebay-area-chart"]>
    <div id=component.getContainerId() class="ebay-area-chart__container"/>
</div>
